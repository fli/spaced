import * as React from 'react';

interface Props {
}

interface State {
}

export default class SignOutButton extends React.Component<Props, State> {

  // handleClick = (event: React.)

  render() {
    return (
      <div>Sign out</div>
    );
  }
}
